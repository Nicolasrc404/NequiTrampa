# Nequi.Finance (core-api)

Pendiente (otra tarea). Contenedor de despliegue: `core-api`.

Categorias, presupuestos y efectivo en Firestore. Las proyecciones de movimientos las escribe `Nequi.Workers` en la coleccion `financial_movements`.
