# Nequi.Profile (core-api)

Pendiente (otra tarea). Contenedor de despliegue: `core-api`.

Perfil y clientes en Spanner. Reutiliza `AddNequiCommon()` / `UseNequiCommon()` de `Nequi.Shared` (auth, health, logging, problem+json).
